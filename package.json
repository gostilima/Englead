{
  "name": "engleap-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd srs-app && npm run dev",
    "build": "cd srs-app && npm run build",
    "lint": "cd srs-app && npm run lint"
  }
}
